# Variáveis usadas no build do app (Vite).
# Copie para .env (desenvolvimento) ou .env.production (produção).
# Não commite .env nem .env.production (contêm segredos).

# Supabase – projeto de PRODUÇÃO (para build de loja)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Desenvolvimento local: pode usar o mesmo projeto ou um projeto de staging.
# Produção: use SEMPRE o projeto Supabase definitivo em .env.production.
